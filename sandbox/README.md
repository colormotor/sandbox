# colormotor_sandbox helpers and utilities

First build C++ bindings:

```
cd lib
mkdir build; cd build
cmake ..
make -j4
```

Likely you have to setup paths in CMakeLists.txt file.

Install with `pip install -e .`


